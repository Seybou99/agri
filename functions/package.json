{
  "name": "agrimali-functions",
  "version": "1.0.0",
  "description": "Cloud Functions SeneGundo (GEE SoilGrids, diagnostic)",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@google/earthengine": "^0.1.350",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "private": true
}
